#
# ratpoison config
# by joshua stein <jcs@jcs.org>
#
# some of these settings require changes from my ratpoison tree:
# https://github.com/jcs/ratpoison
#

# call them as i sees them
set winname title

# keep the bar in the upper left displaying the active frame title
msgwait 3
set barborder 0
set bargravity nw
set barpadding 20 10
set barinpadding 1
set barsticky on
set bgcolor #6c7c87
set fgcolor #e3e0d5
set font monospace:size=11.5
set inputwidth 0
set padding 10 30 10 10
set winliststyle row
set winfmt %t

# add leading and trailing space around this now that the padding is small
set framefmt  frame %f (%wx%h) 

# window borders
set border 5
set gap 10
set fwcolor orange3
set bwcolor gray

# be like screen
escape C-a
bind s split
bind C-s split
bind S hsplit
bind C-S hsplit

bind c exec xterm
bind C-c exec xterm

bind L exec pkill -USR1 xidle

# prevent accidental closes
unbind k

# swap workspaces, but define it in 'top' to avoid having to prefix with C-a
definekey top M-1 vselect 1
definekey top M-2 vselect 2
definekey top M-3 vselect 3
definekey top M-4 vselect 4

# match symbols on the keys of my current laptop as if Fn Lock were on, without
# having to actually enable Fn Lock
definekey top F1 exec mixerctl outputs.master.mute=toggle
definekey top F2 exec mixerctl outputs.master=-10
definekey top F3 exec mixerctl outputs.master=+10
definekey top F5 exec /usr/local/bin/intel_backlight decr
definekey top F6 exec /usr/local/bin/intel_backlight incr
#definekey top F7 exec pkill -USR1 -f "ruby choons"
#definekey top F8 exec pkill -TSTP -f "ruby choons"
#definekey top F9 exec pkill -USR2 -f "ruby choons"

# don't do that
warp off

# i like tab just going between the same two windows
bind Tab focuslast
bind grave focus

# act normal but prevent firefox raising itself when links are opened from
# other apps
rudeness 12

# let popups stay centered
unmanage xmessage
unmanage xprompt
unmanage OpenSSH Authentication Passphrase Request
unmanage dzen title

# startup configuration

# initialize workspaces
vinit
hsplit

exec xterm
