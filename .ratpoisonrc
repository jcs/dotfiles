#
# ratpoison config
# by joshua stein <jcs@jcs.org>
#
# some of these settings require changes from my ratpoison tree:
# http://github.com/jcs/ratpoison
#

# call them as i sees them
set winname title

# reserve space for dzen
set padding 0 17 0 0

# keep the bar in the upper left displaying the active frame title
msgwait 3
set barborder 0
set bargravity nw
set barpadding 2 2
set barsticky on
set bgcolor black
set fgcolor white
set font *-proggytinysz-medium-
set winliststyle row
set winfmt %t
# this prevents it from taking dzen's padding into account
set barinpadding 1

# add leading and trailing space around this now that the padding is small
set framefmt  current frame 

# window borders
set border 2
set fwcolor orange
set bwcolor gray

# be like screen
escape C-a
bind s hsplit
bind C-s hsplit
bind S split
bind C-S split

bind c exec xterm
bind C-c exec xterm

bind L exec xautolock -locknow

# pidgin's window is hard to get to, so control-a,A pops up a dialog to ask me
# what to set the away status to
bind A exec sh ~/bin/pidgin_away_status.sh 
# and B comes back
bind B exec ruby ~/code/pidgin_status/pidgin_status.rb available

# swap workspaces, but define it in 'top' to avoid having to prefix with C-a
definekey top F1 vselect 1
definekey top F2 vselect 2
definekey top F3 vselect 3
definekey top F4 vselect 4
definekey top F5 vselect 5

# control pianobar
definekey top XF86AudioStop exec pgrep pianobar && echo -n p > /var/home/jcs/.config/pianobar/ctl
definekey top XF86AudioPlay exec pgrep pianobar && echo -n p > /var/home/jcs/.config/pianobar/ctl
definekey top XF86AudioNext exec pgrep pianobar && echo -n n > /var/home/jcs/.config/pianobar/ctl

# don't do that
warp off

# i like tab just going between the same two windows
bind Tab focuslast
bind grave focus

# act normal but prevent firefox raising itself when links are opened from
# other apps
rudeness 12

# let popups stay centered
unmanage xmessage
unmanage xprompt

# startup configuration

# initialize the pointer
exec xsetroot -solid black -cursor_name left_ptr

# initialize workspaces
vinit

# split workspace 1 into 3 and start an xterm in the big part
hsplit
focus
split
focus
focus
exec xterm
